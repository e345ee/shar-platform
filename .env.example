# ------------------------------
# Mail configuration (Gmail SMTP example)
# ------------------------------
# IMPORTANT:
# - Do NOT use your Google account password.
# - Use a Google "App Password" (16 chars) as SPRING_MAIL_PASSWORD.
# - Keep this file as an example; create your real .env locally and never commit secrets.

# Enable/disable sending
APP_MAIL_ENABLED=false

# "From" header
APP_MAIL_FROM=no-reply@course.local

# Gmail SMTP (STARTTLS)
SPRING_MAIL_HOST=smtp.gmail.com
SPRING_MAIL_PORT=587
SPRING_MAIL_USERNAME=your_gmail@gmail.com
SPRING_MAIL_PASSWORD=your_google_app_password
SPRING_MAIL_SMTP_AUTH=true
SPRING_MAIL_SMTP_STARTTLS=true
