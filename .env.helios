# Helios deployment environment
# Ports are split into separate variables to avoid hardcoding inside URLs.

# --- Backend ---
SERVER_PORT=9090

# --- Database ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=studs
DB_USER=s368748
DB_PASSWORD=TtSXRLK86yz5ENVZ
DB_SSLMODE=require

# Spring (derived from DB_* by application.yml)
# SPRING_DATASOURCE_URL is no longer required if DB_* is set, but kept here for compatibility:
# SPRING_DATASOURCE_URL=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=${DB_SSLMODE}

SPRING_DATASOURCE_USERNAME=s368748
SPRING_DATASOURCE_PASSWORD=TtSXRLK86yz5ENVZ

SPRING_JPA_PROPERTIES_HIBERNATE_DEFAULT_SCHEMA=s368748

# --- S3/MinIO ---
APP_S3_HOST=127.0.0.1
APP_S3_PORT=9100
APP_S3_REGION=us-east-1
APP_S3_ACCESS_KEY=minio
APP_S3_SECRET_KEY=supersecret123
APP_S3_BUCKET=course

APP_S3_PUBLIC_HOST=192.168.10.80
APP_S3_PUBLIC_PORT=9100

# --- Mail ---
APP_MAIL_FROM=no-reply@course.local
APP_MAIL_ENABLED=false

SPRING_MAIL_HOST=smtp.gmail.com
SPRING_MAIL_PORT=587
SPRING_MAIL_USERNAME=gsad1030@gmail.com
SPRING_MAIL_PASSWORD=foyyffqaxsnshfdw //Заменить
SPRING_MAIL_SMTP_AUTH=true
SPRING_MAIL_SMTP_STARTTLS=true
